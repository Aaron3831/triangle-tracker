$(document).ready(function() {
  $("form").submit(function(event) {
    event.preventDefault();

    var ab = parseInt($("input#ab").val());
    var bc = parseInt($("input#bc").val());
    var ac = parseInt($("input#ac").val());

    {
    if (ab)
      if (bc && ac)

    if (ab === bc === ac) {
      alert("you have an equilateral");
    }

  };
  });
  });
